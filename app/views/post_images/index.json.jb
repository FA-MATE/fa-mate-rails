@post_images.map do |post_image|
  {
    id: post_image.id,
    post_id: post_image.post_id,
    image: post_image.image,
    order_no: post_image.order_no,
    url: post_image_url(post_image, format: :json)
  }
end
